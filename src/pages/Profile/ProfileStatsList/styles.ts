import styled from 'styled-components';

export const ProfileStatsListContainer = styled.div`
  .heading {
    margin-bottom: 12px;
    font-size: ${(props) => props.theme.fontSizes.body.medium};
    font-weight: 600;
  }
`;
